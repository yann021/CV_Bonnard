<%= form_with(model: developement, local: true) do |form| %>
  <% if developement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(developement.errors.count, "error") %> prohibited this developement from being saved:</h2>

      <ul>
      <% developement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :developmentDate %>
    <%= form.text_field :developmentDate %>
  </div>

  <div class="field">
    <%= form.label :website %>
    <%= form.text_field :website %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :mtype %>
    <%= form.number_field :mtype %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
